% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics_formula.R
\name{update.felm}
\alias{update.felm}
\title{Update a fitted \code{felm} model}
\usage{
\method{update}{felm}(object, formula. = . ~ ., vcov = NULL, ...)
}
\arguments{
\item{object}{A fitted \code{felm} object.}

\item{formula.}{Update formula; only the segments you want to change need to
differ from \code{.}.  Examples:
\itemize{
\item \code{. ~ . | country + year} — change FE, keep regressors.
\item \code{. ~ . | . | ctry1 + ctry2} — keep FE, change cluster.
\item \code{. ~ . - bothin | year} — drop a regressor, keep FE.
}}

\item{vcov}{Optional new \code{vcov} value (e.g. \code{"cluster"}).  If omitted the
original value is reused.}

\item{...}{Additional arguments forwarded to \code{\link[=felm]{felm()}}.}
}
\value{
A refitted \code{felm} object.
}
\description{
S3 method for \code{\link[=update]{update()}} that understands the \code{|}-separated formula syntax
used by \code{\link[=felm]{felm()}}.  R's built-in \code{\link[=update.formula]{update.formula()}} breaks on these formulas
because the \code{|} parts look like factor arithmetic.  This method splits the
formula into its three segments, applies updates to each segment
independently, reassembles, and re-fits the model.

The \code{.} placeholder works as usual:
\itemize{
\item \code{. ~ .} — keep the current response and RHS regressors.
\item The second \code{|} segment replaces (or keeps, if \code{.}) the fixed-effects.
\item The third \code{|} segment replaces (or keeps, if \code{.}) the cluster variables.
}
}
