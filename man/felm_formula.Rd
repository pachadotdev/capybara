% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics_formula.R
\name{felm_formula}
\alias{felm_formula}
\title{Build a felm / feglm formula from parts}
\usage{
felm_formula(base, fe = NULL, cluster = NULL)
}
\arguments{
\item{base}{A one-sided or two-sided \code{formula} object \strong{or} a character
string that can be coerced to one, e.g.
\code{"ltrade ~ bothin + onein + gsp"}.}

\item{fe}{Optional character string naming the fixed-effect variable(s),
e.g. \code{"year"} or \code{"country + year"}.  Omit or pass \code{NULL} for models
without fixed effects.}

\item{cluster}{Optional character string naming the cluster variable(s),
e.g. \code{"pair"} or \code{"ctry1 + ctry2"}.  Omit or pass \code{NULL} for models
without explicit clustering.}
}
\value{
A \code{formula} of the form \code{y ~ x}, \code{y ~ x | fe}, or
\code{y ~ x | fe | cluster} depending on which parts are supplied.
}
\description{
Constructs a \code{|}-separated formula accepted by \code{\link[=felm]{felm()}} and \code{\link[=feglm]{feglm()}} from
a base \code{y ~ x} formula plus optional fixed-effects and cluster components
supplied as plain character strings.  This avoids calling \code{\link[=update.formula]{update.formula()}},
which cannot handle the \code{|} syntax and raises \emph{"+ not meaningful for
factors"} errors.
}
\examples{
rhs <- "ltrade ~ bothin + onein + lrgdp"

felm_formula(rhs)
#> ltrade ~ bothin + onein + lrgdp

felm_formula(rhs, fe = "year")
#> ltrade ~ bothin + onein + lrgdp | year

felm_formula(rhs, fe = "year", cluster = "ctry1 + ctry2")
#> ltrade ~ bothin + onein + lrgdp | year | ctry1 + ctry2

}
