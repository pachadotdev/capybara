% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_helpers.R
\name{fit_control}
\alias{fit_control}
\title{Set \code{feglm} and \code{felm} Control Parameters}
\usage{
fit_control(
  dev_tol = 1e-08,
  demean_tol = 1e-08,
  collin_tol = 1e-07,
  iter_max = 25L,
  iter_max_cluster = 100L,
  iter_full_dicho = 10L,
  iter_demean_max = 10000L,
  iter_inner_max = 50L,
  iter_interrupt = 1000L,
  iter_ssr = 10L,
  rel_tol_denom = 0.1,
  irons_tuck_eps = 1e-14,
  safe_division_min = 1e-12,
  safe_log_min = 1e-12,
  newton_raphson_tol = 1e-08,
  convergence_iter_max = 100L,
  convergence_iter_full_dicho = 10L,
  step_halving_factor = 0.5,
  binomial_mu_min = 0.001,
  binomial_mu_max = 0.999,
  safe_clamp_min = 1e-15,
  safe_clamp_max = 1e+12,
  iter_nb_theta = 10L,
  direct_qr_threshold = 0.9,
  qr_collin_tol_multiplier = 1e-07,
  chol_stability_threshold = 1e-12,
  alpha_convergence_tol = 1e-08,
  alpha_iter_max = 10000L,
  demean_extra_projections = 0L,
  demean_warmup_iterations = 15L,
  demean_projections_after_acc = 5L,
  demean_grand_acc_frequency = 20L,
  demean_ssr_check_frequency = 40L,
  keep_dmx = FALSE,
  use_weights = TRUE
)
}
\arguments{
\item{dev_tol}{tolerance level for the first stopping condition of the
maximization routine. The stopping condition is based on the relative change
of the deviance in iteration \eqn{r} and can be expressed as follows:
\eqn{|dev_{r} - dev_{r - 1}| / (0.1 + |dev_{r}|) < tol}{|dev - devold| /
 (0.1 + |dev|) < tol}. The default is \code{1.0e-08}.}

\item{demean_tol}{tolerance level for the stopping condition of the demeaning
algorithm. The default is \code{1.0e-08}.}

\item{collin_tol}{tolerance level for detecting collinearity. The default is
\code{1.0e-07}.}

\item{iter_max}{maximum number of GLM iterations. The default is \code{25L}.}

\item{iter_max_cluster}{maximum number of iterations for cluster
coefficient convergence in (negative) binomial models. The default is \code{100L}.}

\item{iter_full_dicho}{maximum number of full Newton-Raphson iterations
before switching to dichotomy for cluster coefficient convergence in
(negative) binomial models. The default is \code{10L}.}

\item{iter_demean_max}{unsigned integer indicating the maximum number of
iterations in the demeaning algorithm. The default is \code{10000L}.}

\item{iter_inner_max}{maximum number of step-halving iterations. The default
is \code{50L}.}

\item{iter_interrupt}{interruption frequency for user interrupt checks.
The default is \code{1000L}.}

\item{iter_ssr}{frequency for SSR-based convergence checks. The default
is \code{10L}.}

\item{rel_tol_denom}{denominator for relative tolerance in convergence
criterion. The default is \code{0.1}.}

\item{irons_tuck_eps}{tolerance for Irons-Tuck acceleration numerical
convergence. The default is \code{1.0e-14}.}

\item{safe_division_min}{minimum value for safe division operations.
The default is \code{1.0e-12}.}

\item{safe_log_min}{minimum value for safe logarithm operations.
The default is \code{1.0e-12}.}

\item{newton_raphson_tol}{tolerance for Newton-Raphson convergence.
The default is \code{1.0e-08}.}

\item{convergence_iter_max}{maximum iterations for cluster coefficient
convergence (Newton-Raphson + dichotomy). The default is \code{100L}.}

\item{convergence_iter_full_dicho}{number of iterations using full
Newton-Raphson before switching to dichotomy. The default is \code{10L}.}

\item{step_halving_factor}{step size reduction factor in step-halving
algorithm. The default is \code{0.5}.}

\item{binomial_mu_min}{minimum value for mu in binomial family. The default
is \code{0.001}.}

\item{binomial_mu_max}{maximum value for mu in binomial family. The default
is \code{0.999}.}

\item{safe_clamp_min}{minimum value for safe clamping operations. The default
is \code{1.0e-15}.}

\item{safe_clamp_max}{maximum value for safe clamping operations. The default
is \code{1.0e12}.}

\item{iter_nb_theta}{integer indicating the maximum number of iterations for
\code{\link[MASS]{theta.ml}}. The default is \code{25L}.}

\item{direct_qr_threshold}{threshold for using direct QR vs Cholesky
decomposition. The default is \code{0.9}.}

\item{qr_collin_tol_multiplier}{multiplier for QR collinearity tolerance.
The default is \code{1.0e-7}.}

\item{chol_stability_threshold}{threshold for Cholesky stability check.
The default is \code{1.0e-12}.}

\item{alpha_convergence_tol}{tolerance for fixed effects (alpha) convergence.
The default is \code{1.0e-08}.}

\item{alpha_iter_max}{maximum iterations for fixed effects computation.
The default is \code{10000L}.}

\item{demean_extra_projections}{number of extra projections in demeaning
algorithm. The default is \code{0L}.}

\item{demean_warmup_iterations}{number of warmup iterations in demeaning.
The default is \code{15L}.}

\item{demean_projections_after_acc}{projections after acceleration in
demeaning. The default is \code{5L}.}

\item{demean_grand_acc_frequency}{frequency of grand acceleration in
demeaning. The default is \code{20L}.}

\item{demean_ssr_check_frequency}{frequency of SSR checks in demeaning.
The default is \code{40L}.}

\item{keep_dmx}{logical indicating if the demeaned design matrix should be
stored. The demeaned design matrix is required for some covariance
estimators, bias corrections, and average partial effects. This option saves
some computation time at the cost of memory. The default is \code{FALSE}.}

\item{use_weights}{logical indicating whether to use weights. If \code{FALSE},
weights are ignored for performance. The default is \code{TRUE}.}
}
\value{
A named list of control parameters.
}
\description{
Set and change parameters used for fitting \code{\link{feglm}}
and \code{felm}. Termination conditions are similar to
\code{\link[stats]{glm}}.
}
\examples{
fit_control(dev_tol = 1e-6, demean_tol = 1e-6)

}
\seealso{
\code{\link{feglm}}
}
