% Generated by tinyroxygen: do not edit by hand
% Please edit documentation in fixed_effects.R
\name{fixed_effects}
\alias{fixed_effects}
\title{Extract fixed effects from a fitted model}
\usage{
fixed_effects(object, which = NULL)
}

\arguments{
\item{object}{A \code{felm} or \code{feglm} object fitted with
  \code{control = fit_control(return_fe = TRUE)} (the default).}

\item{which}{Optional string with the name of a single fixed-effect
  dimension to extract (e.g. \code{"exp_year"}). If \code{NULL} (default),
  the full named list of fixed-effect vectors is returned.}
}

\value{
A named numeric vector (single dimension) or a named list of
  numeric vectors (all dimensions).
}

\description{
Accessor for the fixed-effect coefficients stored in a
  \code{felm} or \code{feglm} object, so that they can be retrieved
  without reaching into the model object's internal list structure (e.g.
  \code{object$fixed_effects$dimension}).
}

\examples{
ross2004_subset <- ross2004[ross2004$year == 1999, ]
fit <- felm(ltrade ~ ldist | ctry1, ross2004_subset)

fixed_effects(fit, "ctry1")
}

