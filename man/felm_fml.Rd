% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics_formula.R
\name{felm_fml}
\alias{felm_fml}
\title{Create a \code{|}-aware formula for felm / feglm}
\usage{
felm_fml(formula)
}
\arguments{
\item{formula}{A formula of the form \code{y ~ x}, \code{y ~ x | fe}, or
\code{y ~ x | fe | cluster}.}
}
\value{
The same formula with an additional \code{"felm_formula"} class prepended.
}
\description{
Wraps a \code{|}-separated formula in the subclass \code{"felm_formula"} so that
\code{\link[=update]{update()}} dispatches to \code{\link[=update.felm_formula]{update.felm_formula()}} instead of base R's
\code{\link[=update.formula]{update.formula()}}, which cannot handle the \code{|} syntax and raises
\emph{"'|' not meaningful for factors"} errors.

The returned object is fully compatible with \code{\link[=felm]{felm()}} and \code{\link[=feglm]{feglm()}}.
}
\examples{
fml <- felm_fml(ltrade ~ bothin + lrgdp | year)

# update() now works safely on fml
update(fml, . ~ . | . | pair)
#> ltrade ~ bothin + lrgdp | year | pair

}
