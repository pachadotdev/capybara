% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics_formula.R
\name{update.formula}
\alias{update.formula}
\title{\code{|}-aware \code{update} for formulas}
\usage{
\method{update}{formula}(object, formula., ...)
}
\arguments{
\item{object}{A \code{formula} object, possibly containing \code{|} separators.}

\item{formula.}{The update formula, e.g. \code{. ~ . | . | ctry1 + ctry2}.}

\item{...}{Passed to \code{\link[stats:update.formula]{stats::update.formula()}} for plain formulas.}
}
\value{
An updated \code{formula}.
}
\description{
Overrides \code{\link[stats:update.formula]{stats::update.formula()}} so that formulas containing \code{|}
(i.e. the fixed-effect / cluster syntax used by \code{\link[=felm]{felm()}} and \code{\link[=feglm]{feglm()}})
can be updated with the usual \code{.} placeholder syntax without triggering
\emph{"'|' not meaningful for factors"} errors.

Formulas that contain no \code{|} are forwarded unchanged to
\code{\link[stats:update.formula]{stats::update.formula()}}, so existing code is unaffected.
}
