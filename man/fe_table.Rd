% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fe_table.R
\name{fe_table}
\alias{fe_table}
\title{Generate fixed effects tables}
\usage{
fe_table(
  ...,
  n = 5,
  coef_digits = 3,
  latex = FALSE,
  model_names = NULL,
  caption = NULL,
  label = NULL,
  position = "htbp"
)
}
\arguments{
\item{...}{One or more model objects of \code{felm} or \code{feglm} class
fitted with \code{return_fe = TRUE} in \code{control}.}

\item{n}{Number of levels to show per fixed effect dimension. Use
\code{Inf} to show all levels. The default is 5.}

\item{coef_digits}{Number of digits for fixed effect coefficients. The
default is 3.}

\item{latex}{Whether to output as LaTeX code. The default is \code{FALSE}.}

\item{model_names}{Optional vector of custom model names.}

\item{caption}{Optional caption for the table (LaTeX only).}

\item{label}{Optional label for cross-referencing (LaTeX only).}

\item{position}{LaTeX float position specifier (LaTeX only). The default
is \code{"htbp"}.}
}
\value{
A formatted fixed effects table of class \code{summary_table}.
}
\description{
Generate fixed effects tables
}
\examples{
yotov2017_subset <- yotov2017[yotov2017$year == 2006, ]
m1 <- felm(trade ~ log_dist | exp_year, yotov2017_subset)
m2 <- fepoisson(trade ~ log_dist | exp_year, yotov2017_subset)
fe_table(m1, m2, model_names = c("Linear", "Poisson"))
}
